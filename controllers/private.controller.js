const router = require("express").Router();

// Fetch all listing(s) by a user id
router.route("/some-name").get((req, res) => {
  //
});

// Create a new listing by a user id
router.route("/some-name").ppst((req, res) => {
  //
});

// Delete a listing by listing id and user id
router.route("/some-name").delete((req, res) => {
  //
});

module.exports = router;
